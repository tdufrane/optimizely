query GetStartPage($locales: [Locales]) {
  StartPage(locale: $locales) {
    item {
      title
      shortDescription
      keywords
      blocks {
        ...ItemsInContentArea
      }
    }
  }
}
